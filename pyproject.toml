[project]
name = "product-loop"
version = "0.1.0"
description = "Generic governed graph of loops: research, decide, build, feedback"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
required-version = "==0.11.7"

[tool.pytest.ini_options]
testpaths = ["rules"]
